name: docs

on:
  push:
    branches: [main]
  pull_request:

jobs:
  markdown:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 检查 Markdown 相对链接与行尾空白
        run: python3 .github/check-markdown.py
